@model PerformanceEvaluatingApp.Models.Website

@{
    ViewBag.Title = "Tests results for " + Model.Name;
}
<h2 class="text-info">All tests results for website <a href="@Model.Name" target="_blank">@Model.Name</a></h2>

@{ Html.RenderPartial("TestsResults", Model.WebPages);}